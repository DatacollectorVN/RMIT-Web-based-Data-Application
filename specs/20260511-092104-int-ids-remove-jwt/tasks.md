# Tasks: Integer primary keys & remove JWT (SPEC)

**Input**: Design documents from `specs/20260511-092104-int-ids-remove-jwt/`  
**Prerequisites**: plan.md ✅ spec.md ✅ research.md ✅ data-model.md ✅ contracts/ ✅ quickstart.md ✅

**Reminder**: Every place that assumes **UUID** for entity identity must be updated — especially `app/migrations/*.sql`, `app/models/*.py`, and `app/scripts/` (`snapshot_seed.sql`, `reindex_products.py`; verify `migrate.py` / `snapshotdb.py`).

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Parallel-safe (different files, no ordering dependency within the same phase)
- **[US1]** = User Story 1 (numeric IDs) · **[US2]** = no JWT · **[US3]** = cleanup / OpenSearch alignment

---

## Phase 1: Setup (Constitution & top-level docs)

**Purpose**: Align SPEC-00 with this feature before code lands.

- [x] T001 Bump and amend `.specify/memory/constitution.md` to **v2.1** — replace UUID-in-Python and mandatory JWT with BIGINT identity PKs and Phase 1 public API; update §3–§8 sync impact block
- [x] T002 Update `.env.example` — remove or clearly mark `JWT_SECRET` and `JWT_EXPIRY_HOURS` as **optional / Phase 2**; document integer primary keys in a one-line comment
- [x] T003 Update `README.md` — state that entity IDs are integers and Phase 1 API needs no bearer token; point migrations path to `app/migrations/`

---

## Phase 2: Foundational (SQL migrations — blocking)

**Purpose**: PostgreSQL schema uses **BIGINT** identity primary keys and **BIGINT** foreign keys everywhere. No UUID columns for entity IDs.

**⚠️** Complete this phase before ORM / seed / reindex work.

- [x] T004 Rewrite `app/migrations/000002_create_users.up.sql` and `app/migrations/000002_create_users.down.sql` — `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY` (or equivalent); no UUID
- [x] T005 Rewrite `app/migrations/000003_create_products.up.sql` and `app/migrations/000003_create_products.down.sql` — `products` and `photos` tables with BIGINT PKs and `photos.product_id` BIGINT FK → `products(id)`
- [x] T006 Rewrite `app/migrations/000004_create_reviews.up.sql` and `app/migrations/000004_create_reviews.down.sql` — BIGINT PK/FKs to `users` and `products`
- [x] T007 Rewrite `app/migrations/000005_create_orders.up.sql` and `app/migrations/000005_create_orders.down.sql` — BIGINT PK/FK to `users`
- [x] T008 Rewrite `app/migrations/000006_create_order_items.up.sql` and `app/migrations/000006_create_order_items.down.sql` — BIGINT PK/FKs to `orders` and `products`
- [x] T009 Review `app/migrations/000007_create_updated_at_trigger.up.sql` and `app/migrations/000007_create_updated_at_trigger.down.sql` — ensure trigger functions reference no UUID types; adjust only if broken by table recreation
- [x] T010 Validate migration chain: from repo root run `make migratedbreapply` against an empty Postgres and confirm all `up` steps succeed with no UUID type in core tables

---

## Phase 3: User Story 1 — Integer IDs in app service & scripts (Priority: P1) 🎯 MVP

**Goal**: SQLAlchemy models and **all scripts** use integer entity IDs; snapshot seed matches migration DDL.

**Independent Test**: `make migratedbreapply && make snapshotdb` succeeds; `\d users` (or equivalent) shows `bigint` `id`; seed rows use integer literals only.

### ORM (`app/models/`)

- [x] T011 [US1] Refactor `app/models/user.py` — replace `UUID` PK with `BigInteger` + `Identity()` (or `autoincrement=True` per project convention); remove `uuid.uuid4` default
- [x] T012 [P] [US1] Refactor `app/models/product.py` — `Product` and `Photo` BIGINT PKs/FKs as in `data-model.md`
- [x] T013 [P] [US1] Refactor `app/models/review.py` — BIGINT PK/FKs
- [x] T014 [P] [US1] Refactor `app/models/order.py` — `Order` and `OrderItem` BIGINT PK/FKs
- [x] T015 [US1] Update `app/models/__init__.py` — exports unchanged if names unchanged; fix any import side-effects

### Scripts (`app/scripts/`)

- [x] T016 [US1] Rewrite `app/scripts/snapshot_seed.sql` — **only integer** literals for `id`, `user_id`, `product_id`, `order_id`, etc.; update `TRUNCATE` / `INSERT` order to respect FKs; no UUID strings
- [x] T017 [US1] Update `app/scripts/reindex_products.py` — read BIGINT `id` from PostgreSQL; emit JSON **number** `product_id`; bulk `_id` may be `str(int_id)`; remove any `str(uuid)` assumptions; keep placeholder vector logic using string seed based on int id + text fields
- [x] T018 [US1] Audit `app/scripts/migrate.py` — confirm `schema_migrations.version` logic unchanged; no entity UUID parsing; fix only if migrations or paths break
- [x] T019 [US1] Audit `app/scripts/snapshotdb.py` — confirm it still loads `app/scripts/snapshot_seed.sql` after T016; no UUID-specific code paths

---

## Phase 4: User Story 2 — No JWT requirement (Priority: P1)

**Goal**: App starts and serves routes without mandatory JWT env or `Authorization` header.

**Independent Test**: `curl` to `GET /api/v1/health` without headers returns 200; `.env` without `JWT_SECRET` does not prevent startup.

- [x] T020 [US2] Remove `python-jose` / `python-jose[cryptography]` from `app/pyproject.toml` if nothing else imports it
- [x] T021 [US2] Regenerate `app/uv.lock` via `uv lock` after dependency change
- [x] T022 [US2] Update `app/config.py` — drop `JWT_SECRET` / `JWT_EXPIRY_HOURS` or load them only when present without failing defaults
- [x] T023 [US2] Remove or stub unused JWT middleware — delete `app/middleware/auth.py` if present, or ensure `app/main.py` and routers do not require `get_current_user` / Bearer validation
- [x] T024 [US2] Repository grep cleanup — remove dead `jose`, `JWT`, `Bearer` references from `app/` and update any spec snippets under `specs/` that still mandate JWT for Phase 1

---

## Phase 5: User Story 3 — OpenSearch & artefact cleanup (Priority: P2)

**Goal**: Index mapping uses numeric `product_id`; repo does not advertise UUID+JWT for active paths.

**Independent Test**: After `make opensearchinit && make reindexproducts`, inspect one document: `product_id` is a number matching Postgres `products.id`.

- [x] T025 [US3] Add `opensearch/products_mapping.v2.json` — set `product_id` mapping to `{ "type": "long" }`; keep other fields per `contracts/opensearch-product-id.md`
- [x] T026 [US3] Update `opensearch/products_mapping.json` to match v2 (or symlink content) so `init.sh` creates the correct mapping
- [x] T027 [US3] Update `opensearch/init.sh` if index recreation / version bump is required for mapping breaking change
- [x] T028 [P] [US3] Review `app/scripts/search_relevance_check.py` — ensure queries remain valid against `long` `product_id` (usually no change); update comments if they mention UUID
- [x] T029 [US3] Remove or archive stale root-level references to UUID PK + JWT in `docs/` if any remain (optional pass; do not expand scope beyond grep hits)

---

## Phase 6: Polish & cross-cutting

- [ ] T030 Run `specs/20260511-092104-int-ids-remove-jwt/quickstart.md` end-to-end on a clean volume: `docker compose up -d` → `make install` → `make migratedbreapply` → `make snapshotdb` → `make opensearchinit` → `make reindexproducts` → `make relevancecheck` → `curl /api/v1/health` without `Authorization`
- [x] T031 Run `.specify/scripts/bash/update-agent-context.sh cursor-agent` after code + constitution land so `.cursor/rules/specify-rules.mdc` matches v2.1

---

## Dependencies & execution order

| Phase | Depends on | Notes |
|-------|------------|--------|
| 1 Setup | — | Constitution first avoids drift |
| 2 Foundational | Phase 1 | SQL must match v2.1 |
| 3 US1 | Phase 2 | Models/scripts assume BIGINT schema |
| 4 US2 | Phase 3 partial | Can start after T011 (models import); easiest after T019 |
| 5 US3 | T017 | Reindex must emit `long` ids after mapping |
| 6 Polish | All above | Full stack smoke |

### Parallel clusters

```text
# Phase 2 (after T004 starts — separate files):
T005 migrations 000003  ||  T006 000004  ||  T007 000005  ||  T008 000006

# Phase 3 ORM (after T011):
T012 product.py  ||  T013 review.py  ||  T014 order.py

# Phase 5:
T028 search_relevance_check.py  ||  T029 docs grep (if split)
```

---

## Implementation strategy

### MVP (US1 only)

1. Phase 1 + Phase 2 + Phase 3 (T001–T019)  
2. **STOP**: `migratedbreapply` + `snapshotdb` + DB inspection for BIGINT ids  

### Full feature

1. Add Phase 4 (T020–T024)  
2. Add Phase 5 (T025–T029)  
3. Phase 6 validation  

---

## Task counts

| Area | Tasks |
|------|-------|
| Setup | 3 |
| Foundational migrations | 7 |
| US1 (models + scripts) | 9 |
| US2 (JWT removal) | 5 |
| US3 (OpenSearch + cleanup) | 5 |
| Polish | 2 |
| **Total** | **31** |

---

## Notes

- **`app/scripts`**: T016–T019 are mandatory for snapshot/migrate/reindex correctness with integer IDs.
- **`app/migrations`**: T004–T009 must stay consistent with `data-model.md`.
- Dev DB **recreate** (`migratedbreapply` / volume wipe) is expected — document for teammates in PR description.
