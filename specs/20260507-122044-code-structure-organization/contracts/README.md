# Contracts (SPEC-01)

This folder holds **non-HTTP** contracts for the Phase 1 codebase: how local services fit together and where boundaries are drawn.

| Document | Purpose |
|----------|---------|
| [local-runtime.md](./local-runtime.md) | Docker Compose services, ports, and internal URLs (SPEC-00 aligned) |

REST API request/response schemas and endpoint lists belong in later specs (SPEC-04 onward), not here.
